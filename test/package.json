{
  "name": "test",
  "description": "Puppeteer tests for the Tallylab app",
  "main": "puppeteer.js",
  "scripts": {
    "test": "mocha",
    "start": "live-server --no-browser --ignore=test ./.."
  },
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "puppeteer": "^5.5.0"
  }
}
